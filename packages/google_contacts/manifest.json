{
  "id": "google_contacts",
  "version": "1.1.0",
  "kind": "tool",
  "name": "Google Contacts",
  "description": "List, add, edit and delete contacts in a Google account's Contacts.",
  "provides": [
    "get_contacts",
    "add_contact",
    "edit_contact",
    "delete_contact"
  ],
  "requires": [
    "google_auth"
  ],
  "python_requirements": [
    "google-api-python-client>=2.130"
  ],
  "config": [
    {
      "key": "accounts",
      "label": "Allowed accounts",
      "description": "Google account names this tool may use, comma-separated. Leave empty for now and fill it in once you have authorized an account: add accounts with `rona tools run google_auth add_account` or from the dashboard's Settings > Connections > Tool Packages panel, then set this list there too.",
      "type": "list",
      "target": "config",
      "required": false
    }
  ],
  "health_check": ".health:check"
}
