{
  "id": "google_mail",
  "version": "1.0.0",
  "kind": "tool",
  "name": "Google Mail",
  "description": "Send email and read the recent inbox of a Google account via Gmail.",
  "provides": ["send_email", "get_recent_emails"],
  "requires": ["google_auth"],
  "python_requirements": ["google-api-python-client>=2.130"],
  "config": [
    {
      "key": "accounts",
      "label": "Allowed accounts",
      "description": "Google account names this tool may use, comma-separated. Each one needs its own token: run 'python -m toolbox.custom.google_auth.add_account <name>' once per account (opens a browser to authorize).",
      "type": "list",
      "target": "config",
      "required": true
    }
  ],
  "health_check": ".health:check"
}
